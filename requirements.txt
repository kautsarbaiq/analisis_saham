# === Core data & komputasi ===
pandas>=2.2
numpy>=1.26
duckdb>=1.0            # database analitik file-based (backtest cepat)
pyarrow>=16.0          # interop parquet/duckdb

# === Ingestion ===
yfinance>=0.2.40       # harga US & IDX (.JK)
requests>=2.32         # SEC EDGAR (fundamental/insider), GDELT, Telegram
feedparser>=6.0        # RSS berita (Yahoo Finance per-ticker)

# === NLP sentimen ===
vaderSentiment>=3.3    # baseline/fallback
transformers>=4.40     # FinBERT (sentimen finansial, default)
torch                  # backend FinBERT

# === Delivery ===
fastapi>=0.111          # backend API dashboard
uvicorn[standard]>=0.30 # ASGI server
python-dotenv>=1.0      # baca .env

# === Dev / test ===
pytest>=8.0
ruff>=0.5

# Audit 2026-07: dihapus krn tak diimpor di mana pun (terverifikasi grep):
# pandas-ta, pandas-datareader, scikit-learn, scipy, python-telegram-bot
# (alert pakai requests), loguru, pydantic, jinja2. Frontend chart via CDN.
